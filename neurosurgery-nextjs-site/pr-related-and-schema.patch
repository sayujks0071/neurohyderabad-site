*** Begin Patch
*** Update File: src/lib/seo.ts
@@
-export const SITE_URL = "https://www.drsayuj.com";
+export const SITE_URL = "https://www.drsayuj.com";
 export const CONTACT_EMAIL = process.env.NEXT_PUBLIC_CONTACT_EMAIL || "contact@drsayujkrishnan.com";
 
+// Existing helpers assumed:
+// - breadcrumbJsonLd
+// - conditionJsonLd
+// - physicianJsonLd
+// - procedureJsonLd
+// - webPageJsonLd
+
+// Enhance webPageJsonLd to support dates and mainEntity
+// If your existing function already accepts an object, this augments it.
+export function webPageJsonLd(args: {
+  name: string;
+  description?: string;
+  url: string;
+  datePublished?: string; // ISO
+  dateModified?: string;  // ISO
+  mainEntity?: any;       // Link to the primary MedicalProcedure/MedicalCondition/Service JSON-LD
+}) {
+  const { name, description, url, datePublished, dateModified, mainEntity } = args;
+  const base: any = {
+    "@context": "https://schema.org",
+    "@type": "WebPage",
+    name,
+    url
+  };
+  if (description) base.description = description;
+  if (datePublished) base.datePublished = datePublished;
+  if (dateModified) base.dateModified = dateModified;
+  if (mainEntity) base.mainEntity = mainEntity;
+  return base;
+}
+
+// New: Service JSON-LD helper
+export function serviceJsonLd(params: {
+  name: string;
+  url: string;
+  description?: string;
+  serviceType?: string;
+  areaServed?: string[]; // e.g., ["Hyderabad", "Telangana", "India"]
+  providerName?: string; // defaults to Dr. Sayuj Krishnan
+  providerUrl?: string;  // defaults to SITE_URL
+}) {
+  const {
+    name,
+    url,
+    description,
+    serviceType,
+    areaServed,
+    providerName = "Dr. Sayuj Krishnan",
+    providerUrl = SITE_URL
+  } = params;
+  return {
+    "@context": "https://schema.org",
+    "@type": "Service",
+    name,
+    serviceType: serviceType || name,
+    url,
+    ...(description ? { description } : {}),
+    provider: {
+      "@type": "Physician",
+      name: providerName,
+      url: providerUrl,
+      medicalSpecialty: "Neurosurgery"
+    },
+    ...(areaServed ? { areaServed } : {})
+  };
+}
+
+// New: MedicalGuideline JSON-LD helper
+export function medicalGuidelineJsonLd(params: {
+  name: string;
+  url: string;
+  subject?: string; // e.g., "Lumbar Disc Herniation" or "Endoscopic Spine Surgery"
+}) {
+  const { name, url, subject } = params;
+  return {
+    "@context": "https://schema.org",
+    "@type": "MedicalGuideline",
+    name,
+    url,
+    ...(subject
+      ? { guidelineSubject: { "@type": "MedicalEntity", name: subject } }
+      : {})
+  };
+}
+
+// New: ContactPoint JSON-LD helper
+export function contactPointJsonLd(params: {
+  phone: string;
+  contactType: string; // e.g., "appointments"
+  areaServed?: string[]; // e.g., ["IN"]
+  availableLanguage?: string[]; // e.g., ["en", "hi", "te"]
+}) {
+  const { phone, contactType, areaServed, availableLanguage } = params;
+  return {
+    "@context": "https://schema.org",
+    "@type": "ContactPoint",
+    telephone: phone,
+    contactType,
+    ...(areaServed ? { areaServed } : {}),
+    ...(availableLanguage ? { availableLanguage } : {})
+  };
+}
*** End Patch

